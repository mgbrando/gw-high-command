import * as actions from '../actions/activitiesActions';

const initialRepositoryState = {
	tasks: [],
	showAddTaskModal: false,
	showReviewTaskModal: false
};

const activities = (state=initialRepositoryState, action) => {
	return state;
};

export default activities;