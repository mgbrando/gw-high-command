import * as actions from '../actions/dashboardActions';

const initialRepositoryState = {
	slideIndex: 0
};

const dashboard = (state=initialRepositoryState, action) => {

	return state;
};

export default dashboard;